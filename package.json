{
  "name": "radix-autocomplete",
  "private": false,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "start": "concurrently -k -n FRONTEND,BACKEND -c green,blue \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "start:frontend": "npm run dev --workspace frontend",
    "start:backend": "npm run dev --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
